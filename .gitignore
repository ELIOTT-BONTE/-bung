node_modules
dist
dist-ssr
coverage
*.local

# API keys. `.env.example` is the committed template; the rest hold real keys.
.env
.env.*
!.env.example

# Model weights and caches must never be committed.
models/
*.gguf
*.wasm.cache
.cache/

.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.log
npm-debug.log*
